apiVersion: apps/v1
kind: Deployment
metadata:
  name: d1-nginx
  labels:
    app: d1-nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: d1-nginx
  template:
    metadata:
      labels:
        app: d1-nginx
    spec:
      containers:
        - name: nginx
          image: nginx:stable-alpine
          ports:
            - containerPort: 80
