apiVersion: apps/v1
kind: Deployment
metadata:
  name: b1-nginx
  labels:
    app: b1-nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: b1-nginx
  template:
    metadata:
      labels:
        app: b1-nginx
    spec:
      containers:
        - name: nginx
          image: nginx:stable-alpine
          ports:
            - containerPort: 80
