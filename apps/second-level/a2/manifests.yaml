apiVersion: apps/v1
kind: Deployment
metadata:
  name: a2-nginx
  labels:
    app: a2-nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: a2-nginx
  template:
    metadata:
      labels:
        app: a2-nginx
    spec:
      containers:
        - name: nginx
          image: nginx:stable-alpine
          ports:
            - containerPort: 80
