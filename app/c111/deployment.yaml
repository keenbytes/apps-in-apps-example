apiVersion: apps/v1
kind: Deployment
metadata:
  name: c111-nginx
  labels:
    app: c111-nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: c111-nginx
  template:
    metadata:
      labels:
        app: c111-nginx
    spec:
      containers:
        - name: nginx
          image: nginx:stable-alpine
          ports:
            - containerPort: 80
