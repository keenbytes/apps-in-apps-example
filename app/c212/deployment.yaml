apiVersion: apps/v1
kind: Deployment
metadata:
  name: c212-nginx
  labels:
    app: c212-nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: c212-nginx
  template:
    metadata:
      labels:
        app: c212-nginx
    spec:
      containers:
        - name: nginx
          image: nginx:stable-alpine
          ports:
            - containerPort: 80
