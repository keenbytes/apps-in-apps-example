apiVersion: apps/v1
kind: Deployment
metadata:
  name: c211-nginx
  labels:
    app: c211-nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: c211-nginx
  template:
    metadata:
      labels:
        app: c211-nginx
    spec:
      containers:
        - name: nginx
          image: nginx:stable-alpine
          ports:
            - containerPort: 80
