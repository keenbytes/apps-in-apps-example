apiVersion: apps/v1
kind: Deployment
metadata:
  name: a11-nginx
  labels:
    app: a11-nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: a11-nginx
  template:
    metadata:
      labels:
        app: a11-nginx
    spec:
      containers:
        - name: nginx
          image: nginx:stable-alpine
          ports:
            - containerPort: 80
