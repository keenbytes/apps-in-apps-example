apiVersion: apps/v1
kind: Deployment
metadata:
  name: a21-nginx
  labels:
    app: a21-nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: a21-nginx
  template:
    metadata:
      labels:
        app: a21-nginx
    spec:
      containers:
        - name: nginx
          image: nginx:stable-alpine
          ports:
            - containerPort: 80
