apiVersion: apps/v1
kind: Deployment
metadata:
  name: c112-nginx
  labels:
    app: c112-nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: c112-nginx
  template:
    metadata:
      labels:
        app: c112-nginx
    spec:
      containers:
        - name: nginx
          image: nginx:stable-alpine
          ports:
            - containerPort: 80
